PROBLEMAS Y CAREncias DETECTADOS EN FOLIARIUM
==============================================
Fecha de revisión: junio 2026
Contexto: el repositorio local es entorno de desarrollo/pruebas previo al despliegue
en el servidor de la universidad. Carpetas ignoradas (data/, imagenes/, experiments/,
models/, etc.) contienen datos de prueba, no el contenido final de producción.

Leyenda de criticidad
---------------------
  [CRÍTICO]   Debe resolverse antes de la entrega o puede invalidar la defensa
              (fallo funcional, seguridad grave en producción, incoherencia grave).
  [ALTO]      Conviene resolverlo antes de la entrega; si no, documentar explícitamente
              en la memoria y asumir el riesgo en la defensa.
  [MEDIO]     Mejora recomendable; puede dejarse si se justifica y no afecta al objetivo
              principal del TFG.
  [BAJO]      Deuda técnica o estética; puede posponerse sin impacto relevante en la entrega.
  [N/A]       No requiere acción (fuera de control, ya resuelto en producción, o solo aplica
              a la redacción de la memoria).


SEGURIDAD Y AUTENTICACIÓN
-------------------------

1. Hash de contraseñas incompleto o híbrido
   Descripción: el cliente (logicav3.py) aplica un hash casero con sal fija "HOLAAAA"
   antes de enviar la contraseña. El servidor aplica bcrypt sobre ese valor recibido,
   no sobre la contraseña en texto plano. Funciona si el flujo es coherente en registro
   y login, pero no es un esquema estándar y puede generar confusión en la defensa.
   Criticidad: [ALTO]
   Antes de la entrega: SÍ, conviene revisar que registro, login y cambio de contraseña
   funcionen de extremo a extremo en el servidor de producción. Si el esquema actual es
   intencional, documentarlo en la memoria (cap. implementación / seguridad). Si quedó a
   medias por olvido, unificar (p. ej. enviar contraseña por HTTPS y hashear solo en servidor).

2. JWT_SECRET con valor por defecto en código
   Descripción: main.py y setup_bbdd.py usan "CHANGE_ME_IN_PRODUCTION" si no hay .env.
   Criticidad: [BAJO] en producción (ya configurado en el servidor de la universidad).
   Antes de la entrega: NO es urgente si producción ya tiene JWT_SECRET en .env. Sí
   comprobar que el .env.docker.example o la documentación adviertan de no desplegar sin
   cambiarlo (solo por si alguien clona y levanta sin leer).

3. verify=False en peticiones HTTP del cliente
   Descripción: logicav3.py y partes de main_app.py desactivan verificación SSL.
   Motivo conocido: evitar errores en desarrollo con certificados autofirmados.
   Criticidad: [MEDIO] en builds/cliente que apunten a producción con HTTPS real.
   Antes de la entrega: RECOMENDABLE quitar verify=False (o hacerlo configurable:
   False en dev, True en producción) en la versión que se distribuya contra el servidor
   universitario. No bloquea la entrega si la app de escritorio en LAN funciona, pero
   es un punto que puede preguntarse en la defensa.

4. Token JWT en query string (access_token / token)
   Descripción: la API acepta el token en la URL además del header Authorization
   (útil para abrir imágenes en navegador/móvil). Riesgo: tokens en logs, historial o
   referrers.
   Criticidad: [MEDIO]
   Antes de la entrega: NO obligatorio corregir. SÍ mencionar en memoria como limitación
   conocida o decisión de diseño para compatibilidad con Flet/móvil.

5. Imágenes y datos sensibles en repositorio
   Descripción: .gitignore cubre ssl_certs, .env, backups, etc. Correcto.
   Criticidad: [BAJO] si se mantiene la disciplina actual.
   Antes de la entrega: NO requiere cambio de código. Revisar una vez que no se suba
   nada sensible por error antes de entregar el repo al tribunal.


ARQUITECTURA Y CÓDIGO
---------------------

6. Archivos monolíticos (main.py ~2200 líneas, main_app.py ~5600 líneas)
   Descripción: toda la lógica concentrada en pocos ficheros; dificulta mantenimiento.
   Criticidad: [BAJO] para la entrega del TFG.
   Antes de la entrega: NO (decisión del autor: priorizar memoria y ajustes necesarios).
   Aceptable si el sistema funciona en producción.

7. Entrenamiento de modelos vía subprocess desde la API
   Descripción: /entrenar_modelo lanza scripts externos; puede bloquear workers de
   Gunicorn durante entrenamientos largos.
   Criticidad: [MEDIO] operativo en producción.
   Antes de la entrega: NO obligatorio refactorizar (cola de trabajos, Celery, etc.).
   SÍ documentar en memoria el flujo y advertir que entrenamientos largos conviene
   lanzarlos por scripts/notebooks en el servidor, no necesariamente desde la app.

8. Sin tests automatizados ni CI
   Descripción: no hay pytest ni pipelines en .github/.
   Criticidad: [MEDIO] académico (reproducibilidad y rigor).
   Antes de la entrega: NO obligatorio implementar suite completa. SÍ incluir en memoria
   un apartado de validación manual (por rol, subida, etiquetado, predicción, Docker)
   con los pasos que ya habéis hecho en servidor.

9. Dependencia requirements.txt
   Descripción: el Dockerfile referencia requirements.txt; verificar que exista y esté
   actualizado en el repo que se entrega o en el que se despliega.
   Criticidad: [ALTO] si falta o está desactualizado (Docker no construye).
   Antes de la entrega: SÍ, comprobar que el despliegue en servidor se reproduce con
   pip install -r requirements.txt o docker compose build sin sorpresas.


DATOS Y EXPERIMENTACIÓN
-----------------------

10. Pocos datos reales
    Descripción: volumen limitado de imágenes en condiciones de campo.
    Criticidad: [N/A] — fuera de control del autor.
    Antes de la entrega: NO requiere acción técnica. SÍ reflejar en memoria como
    limitación explícita y enfoque del TFG (plataforma + experimentación exploratoria).

11. Carpetas ignoradas vacías o con datos de prueba en local
    Descripción: data/, imagenes/, experiments/, models/ no van al repo; en producción
    se regeneran con el uso normal de la app.
    Criticidad: [N/A] — comportamiento esperado.
    Antes de la entrega: NO cambiar el enfoque. SÍ dejar en memoria/apéndices cómo
    inicializar el sistema (setup_bbdd, docker compose --profile init, primer uso).


DOCUMENTACIÓN Y MEMORIA
-----------------------

12. Nombre del proyecto: Foliarium (opcional énfasis FolIArium en memoria)
    Descripción: código y README usan Foliarium; borradores antiguos usaban PlantAID.
    Criticidad: [BAJO] en código; [MEDIO] en memoria final.
    Antes de la entrega: NO tocar READMEs (decisión del autor). SÍ usar Foliarium de
    forma consistente en estructura_memoria_tfg.tex al redactar.

13. main.tex obsoleto e incompleto
    Descripción: era almacén provisional de explicaciones; describe arquitectura antigua
    (FastAPI + Flask separados), rutas de scripts incorrectas, sección de documentación
    desactualizada.
    Criticidad: [N/A] como problema del producto — solo afecta si se usara como memoria final.
    Antes de la entrega: NO actualizar main.tex. Trabajar solo en estructura_memoria_tfg.tex
    (formato tfgetsinf oficial). Reciclar párrafos de main.tex con cuidado y contrastando
    con el estado actual del repositorio/servidor.

14. docs/estructura_memoria_tfg.tex vs memoria/estructura_memoria_tfg.tex
    Descripción: puede haber dos copias del esqueleto; la de trabajo debería ser la de
    memoria/ (gitignored) o una única fuente de verdad acordada.
    Criticidad: [BAJO]
    Antes de la entrega: decidir qué carpeta es la maestra para no editar dos veces.


OPERACIÓN Y DESPLIEGUE (PRODUCCIÓN UNIVERSITARIA)
-------------------------------------------------

15. Backups de MongoDB no automatizados en Docker Compose
    Descripción: existe scripts/backup_mongo.sh pero no está integrado en compose ni cron.
    Criticidad: [MEDIO] para operación del servidor; [BAJO] para la entrega académica.
    Antes de la entrega: NO obligatorio automatizar. Opcional mencionar en memoria o
    anexo de despliegue.

16. TLS: Gunicorn HTTP interno + proxy inverso
    Descripción: diseño correcto para producción; en local se usa HTTPS con certs
    autofirmados (run_server.py).
    Criticidad: [N/A] — ya contemplado.
    Antes de la entrega: describir en memoria el despliegue real del servidor UPV.


RESUMEN: QUÉ SÍ RESOLVER O VERIFICAR ANTES DE LA ENTREGA
---------------------------------------------------------
  Obligatorio / muy recomendable:
    - [1] Revisar flujo de autenticación (hash) en producción y documentarlo.
    - [9] Verificar requirements.txt y despliegue reproducible en servidor. HECHO
    - [3] Valorar quitar o condicionar verify=False en cliente de producción.
    - Redactar estructura_memoria_tfg.tex (no main.tex) con arquitectura ACTUAL.

  Documentar en memoria sin obligación de refactor:
    - [4] Token en query string.
    - [7] Entrenamiento vía API vs scripts.
    - [8] Validación manual (sin tests automatizados).
    - [10] Limitación de datos reales.
    - [11] Inicialización de carpetas y BBDD en producción.
    - [16] Despliegue Docker + servidor universitario. HECHO

  Posponer explícitamente (acordado):
    - [6] Refactor de monolitos.
    - [12] Cambios en READMEs del repo.
    - [13] Actualización de main.tex.


MEMORIA — ESTADO ACORDADO
-------------------------
  - main.tex: borrador/almacén provisional. NO es la memoria final. Obsoleto.
  - estructura_memoria_tfg.tex: formato correcto (clase tfgetsinf UPV). Es el documento
    sobre el que trabajar por capítulos.
  - Contenido útil de main.tex: solo como material fuente a revisar y adaptar, no copiar
    literal sin contrastar con el código y el servidor actual.
